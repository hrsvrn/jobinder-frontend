import React from 'react'

const AuthModal = ({setShowModal}) => {
    const handleClick=()=>{
        return (
            <div onClick={{handleClick}}>
              
            </div>
          )

    }
 
}

export default AuthModal
